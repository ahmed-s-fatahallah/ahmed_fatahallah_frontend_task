import { Cairo, Inter, Lexend } from "next/font/google";

// Define fonts and export them
export const cairo = Cairo({ subsets: ["latin"] });
export const inter = Inter({ subsets: ["latin"] });
export const lexend = Lexend({ subsets: ["latin"] });
